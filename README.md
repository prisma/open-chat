# Open Chat

Open Chat is a local-first open source chat app modeled after t3.chat. It uses Bun as the app server, Better Auth for user sessions, Prisma Next with local Prisma Postgres for metadata, Prisma Streams for durable chat message logs, TanStack DB for client state, and OpenRouter for model access.

The architecture and implementation plan live in [`docs/`](./docs).

